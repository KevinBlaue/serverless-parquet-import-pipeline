# Serverless Parquet Import Pipeline

This project demonstrates a fully automated AWS workflow for importing large data volumes efficiently. It uses a serverless pipeline to transform incoming data into Parquet, providing a compact, analytics-friendly format without managing persistent infrastructure.
